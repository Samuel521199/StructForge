import type { App } from 'vue'
// Vue Flow 会在需要时按需导入

/**
 * Vue Flow 插件配置
 * 工作流编辑器使用
 */
export function setupVueFlow(app: App) {
  // Vue Flow 不需要全局注册
  // 在使用时按需导入即可
  // import { VueFlow } from '@vue-flow/core'
}

